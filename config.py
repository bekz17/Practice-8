# Database connection settings for local PostgreSQL.
# Adjust credentials to match your environment.

DB_CONFIG = {
    "host": "localhost",
    "database": "phonebook_db",
    "user": "postgres",
    "password": "postgres",
    "port": 5432,
}
